# Deterministic self-check — rebuild03

Artifact build: `ui-20260828-01`

Machine validation: `pending`

Conformance level: `authoring_aligned`

This binary ledger does not upgrade source inspection into rendered evidence. A `fail`
below can mean an applicable browser, destination, or final-state check remains open;
the reason names that boundary.

| ID | Result | Evidence or open reason |
|---|---|---|
| SC-01 | pass | Both locale roots carry the required identity attributes; each head carries all four `landometer:*` receipt metas. |
| SC-02 | pass | The canonical inline theme-init is before every stylesheet and resolves stored light/dark choice to system otherwise. |
| SC-03 | fail | Canonical kit token bytes match SHA-256 `aa834b08…1323`, but the imported canonical `tokens/ext-registry.css` contains 27 raw hex occurrences outside the Appendix E block; the page-local layer adds none. |
| SC-04 | pass | Every source button uses the canonical `.btn` capsule; the two icon-only menu/locale actions also use `.btn-icon` circle geometry. |
| SC-05 | fail | Token/local-contract focus styles exist, but rendered resolution on every surface was blocked by the browser policy. |
| SC-06 | pass | One global reduced-motion block disables animation and transition; no-JavaScript content begins in its final visible state. |
| SC-07 | fail | Source minimums are present, but computed 44×44 targets and final Latin/Thai text sizes were not measured in a rendered browser. |
| SC-08 | pass | Each locale has exactly one H1, matching `lang`, header/main/footer landmarks, and a skip link as its first focusable source element. |
| SC-09 | pass | The build-local layer defines no gradient; atmosphere gradients resolve only through the canonical DS recipes. |
| SC-10 | pass | `site.css` has zero `!important`; all custom properties it defines derive through `var()`. JS reveal delay is calculated from the governed stagger tokens. |
| SC-11 | fail | Fonts and runtime resources are self-hosted and the primary copy is static HTML, but the required fully network-blocked rendered test could not run. |
| SC-12 | fail | This is a public page. It is deliberately `noindex`, but the literal check still requires Open Graph, JSON-LD, sitemap inclusion, and an approved favicon context; those roles are omitted while identity and discovery evidence remain open. |
| SC-13 | n/a | No platform/social share destination is promoted. The post-AHA control only copies an exact evidence link and keeps the visible version/date boundary. |
| SC-14 | fail | The family-level source inventory covers 76 control/selectable occurrences across enhanced and no-script branches, but it is not a per-control final-state receipt and the outcomes were not browser-tested. |
| SC-15 | fail | Local fragments and resources resolve with zero source errors; external destinations were not all destination-tested in a browser. |
| SC-16 | fail | The shared manifest records both locale routes but is not a complete per-route §9.4 projection; full foreground, media, font, approval, and rendered-contract parity remains open. |
| SC-17 | pass | The retired values are absent from the full delivered tree, including comments and documentation; the upstream migration list is referenced without reproducing its literals. |
| SC-18 | pass | The first-view DOM has no caution banner or false alert; the evidence affordance follows the proof block. |
| SC-19 | fail | Source uses one self-hosted Material Symbols Rounded subset at FILL 0 / wght 300 / GRAD 0, but rendered glyph and computed-axis resolution was not browser-tested. |
| SC-20 | fail | Responsive containers and the two-line Thai hero were not inspected at every governed breakpoint and both themes in a rendered browser. |
| SC-21 | fail | Capsule padding and minimums come from the kit, but rendered centring, clipping, and target geometry were not measured. |
| SC-22 | fail | Source has once-only reveals plus reduced-motion/no-JS final states and a landed-state observer, but the rendered entrance acceptance test did not run. |
| SC-23 | fail | The kit supplies inline-flex icon anatomy and `--space-2` gap, but rendered icon/label and circle centres were not measured. |

Totals: 9 pass · 13 fail/open · 1 not applicable.
