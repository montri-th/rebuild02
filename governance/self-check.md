# Deterministic self-check — Rebuild02

Artifact build: `ui-20260830-10`

Machine validation: `pending`

Conformance level: `authoring_aligned`

This ledger keeps rendered, destination and governance exceptions open instead of
upgrading source inspection into stronger evidence.

| ID | Result | Evidence or open reason |
|---|---|---|
| SC-01 | pass | Both locale roots carry the required identity attributes and four `landometer:*` receipt metas. |
| SC-02 | pass | The inline theme initializer precedes every stylesheet and resolves stored light/dark preference or the system theme. |
| SC-03 | fail | Canonical kit bytes match their recorded hashes, but the imported canonical extension registry contains raw values outside Appendix E; the page-local layer adds none. |
| SC-04 | pass | Text actions and the theme/language preferences use capsule geometry; icon-only exits use circles. The topic picker keeps native field geometry and is not treated as a third action shape. |
| SC-05 | pass | Foreground contracts, focus styles, non-modal menu focus/restoration, disclosure, listbox, form and keyboard paths passed local implementation QA. |
| SC-06 | pass | The reduced-motion contract disables reveals, disclosure animation, the hero pan, hero parallax, substantial-static-image parallax and CTA sweep, keeps the navbar prominent, and retains final visible states; no-JavaScript content starts visible. |
| SC-07 | pass | Source minimums are present; local in-app Chromium confirmed prominent/calm navbar geometry, CTA/menu and checked controls fit at 1440×900, 1024×768, 390×844 and 320×720 without overflow. |
| SC-08 | pass | Each locale has one H1, matching language, header/main/footer landmarks and a first-source skip link. |
| SC-09 | pass | Atmosphere gradients resolve through canonical DS recipes. The only page-local raw colour declaration is the owner-approved r6 light-wordmark identity value, centralized once and recorded in the identity contract. |
| SC-10 | fail | `site.css` has three narrowly scoped `!important` declarations needed to normalize TikTok's provider-owned embed width and two local custom properties for header geometry and approved identity colour; this is not a literal all-properties-through-`var()` pass. |
| SC-11 | pass | Primary copy is static HTML and initial page rendering is same-origin/self-hosted. An initial dark theme shows same-origin DS channel cards and initializes no provider surface; light theme attaches official embeds only near their cards. The contact POST occurs only after explicit submit. |
| SC-12 | pass | Both locale routes are indexable and carry canonical/hreflang links, Open Graph and Twitter metadata, Organization/WebSite/WebPage JSON-LD, the r6 favicon, `robots.txt`, and a truthful release-dated sitemap. |
| SC-13 | pass | TikTok, Facebook, Instagram and LinkedIn use minimal brand marks in a readable editorial grid. Initial dark theme shows same-origin DS channel cards without initializing provider surfaces; light theme retains lazy official embeds. The X embed remains omitted while its outbound footer link remains. |
| SC-14 | fail | The 112 native raw / 105 enhanced source inventory plus two keyboard-focusable horizontal rails, local keyboard/form states and explicit 44 px `mailto:` action are checked, but a real contact POST/inbox outcome is not included. Runtime loop clones are sanitized, inert and excluded from focusable-control counts. |
| SC-15 | pass | GitHub Pages deployment `6167529645` succeeded for artifact commit `f9b7826a`; root, index, TH, EN and `_ds` CSS returned HTTP 200, and 9 live resources matched local byte length and SHA-256. Targeted live EN desktop and initial-dark TH mobile rendering verified the corrected media roles, substantial-image parallax, both bidirectional cyclic rails and the same-origin dark social boundary. |
| SC-16 | fail | The shared manifest records both locale routes but is not claimed as a complete per-route rendered-contract projection. |
| SC-17 | pass | Retired values are absent from the public page layer; no retired literal was added in this release. |
| SC-18 | pass | The first view has no caution/caveat block; the dated product proof band follows the hero and the null legacy row is not rendered. |
| SC-19 | pass | All 25 active interface ligatures exist in the 25-glyph self-hosted r10 canonical Material Symbols Rounded file at FILL 0 / wght 300 / GRAD 0. Bookmark roles use unique widgets, monitoring, newspaper and mail outline glyphs; retaining FILL 0 for the current bookmark is the recorded owner override of the r7 handoff's filled-active example. Product and social marks remain brand assets. |
| SC-20 | fail | Targeted local and live EN/TH desktop/mobile checks cover parallax, both cyclic rails, restored CityMETER media and initial-dark/light social behavior, but a complete locale × theme × viewport visual baseline remains open. |
| SC-21 | pass | The navbar is 76/68 px when prominent and scales the complete row to a 29/27 px calm state; it restores on upward scroll, pointer/focus intent and menu open. Mobile shows only the carrier-free brand and circular menu control, while the one-line sign-in action moves inside the full-width menu. |
| SC-22 | pass | Source inspection confirms the approved reveals, story staging, four-beat rally, CTA sweep and 32-second hero pan remain. Hero parallax stays capped at ±20 px; substantial static content images receive separately bounded scroll-linked movement; every layer freezes under reduced motion. Initial dark social uses same-origin DS cards, while light retains lazy provider embeds. The owner-directed ambient video loop remains an explicit divergence from canonical no-loop guidance. |
| SC-23 | pass | Rendered icon/label pairs use the canonical 24 px face and governed gaps; all current ligatures resolve. Location Intelligence and testimonials each retain six semantic source cards plus sanitized inert head/tail clone cycles for bidirectional looping. Five product cards retain 44 px actions; CityMETER restores the responsive Business Dynamics family, and the automotive market-share PNG is assigned only to branch potential. |

Totals: 18 pass · 5 fail/open · 0 not applicable.
