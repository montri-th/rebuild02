# Deterministic self-check — rebuild03

Artifact build: `ui-20260829-01`

Machine validation: `pending`

Conformance level: `authoring_aligned`

This ledger keeps rendered, destination and governance exceptions open instead of
upgrading source inspection into stronger evidence.

| ID | Result | Evidence or open reason |
|---|---|---|
| SC-01 | pass | Both locale roots carry the required identity attributes and four `landometer:*` receipt metas. |
| SC-02 | pass | The inline theme initializer precedes every stylesheet and resolves stored light/dark preference or the system theme. |
| SC-03 | fail | Canonical kit bytes match their recorded hashes, but the imported canonical extension registry contains raw values outside Appendix E; the page-local layer adds none. |
| SC-04 | fail | Primary actions use the canonical capsule, but theme and listbox buttons deliberately use their component-specific geometry rather than `.btn`. |
| SC-05 | fail | Foreground contracts and focus styles exist; a complete keyboard/focus audit on every surface remains open. |
| SC-06 | pass | One global reduced-motion block disables transition/animation and no-JavaScript content starts in its final visible state. |
| SC-07 | pass | Source minimums are present; local Chrome confirmed the navbar CTA/menu and the checked controls fit at 320 and 390 px. |
| SC-08 | pass | Each locale has one H1, matching language, header/main/footer landmarks and a first-source skip link. |
| SC-09 | pass | The page layer defines no raw colour or gradient; atmosphere gradients resolve through canonical DS recipes. |
| SC-10 | fail | `site.css` has zero `!important` and one intentionally local scalar custom property for header height; this is not a literal all-properties-through-`var()` pass. |
| SC-11 | pass | Primary copy is static HTML and all load-time rendering subresources are same-origin/self-hosted; the contact POST occurs only after explicit submit. |
| SC-12 | fail | The public page remains deliberately `noindex`; Open Graph, JSON-LD, sitemap inclusion and install/social icon roles remain outside this release. An explicit same-origin PNG favicon is present. |
| SC-13 | n/a | No social promotion destination is rendered; the compact share control preserves the exact CityMETER proof link. |
| SC-14 | fail | The 77-occurrence source inventory and core keyboard states are checked, but a real contact POST/inbox outcome is not included. |
| SC-15 | fail | Local fragments/resources resolve and API preflight/GET guard pass; all external destinations and inbox delivery were not destination-tested. |
| SC-16 | fail | The shared manifest records both locale routes but is not claimed as a complete per-route rendered-contract projection. |
| SC-17 | pass | Retired values are absent from the public page layer; no retired literal was added in this release. |
| SC-18 | pass | The first view has no caution/caveat block; the dated product proof band follows the hero and the null legacy row is not rendered. |
| SC-19 | pass | All 13 rendered Material Symbols ligatures exist in the self-hosted subset and measured 24×24 in local Chrome. |
| SC-20 | fail | Local Chrome spot-checks cover 1280, 390 and 320 layouts, but a full locale × theme × viewport visual baseline remains open. |
| SC-21 | pass | The navbar remains 76/68 px and local Chrome shows the mobile CTA as one line without horizontal overflow at 320/390 px. |
| SC-22 | fail | Reveals, lazy media, offscreen video attachment and reduced-motion behavior work in the local audit; owner-directed ambient looping is an explicit divergence from the canonical no-loop guidance. |
| SC-23 | pass | Rendered icon/label pairs use the canonical 24 px face and governed gaps; all current ligatures resolve. |

Totals: 12 pass · 10 fail/open · 1 not applicable.
