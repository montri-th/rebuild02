# Deterministic self-check — Rebuild02

Artifact build: `ui-20260830-11` published and live-verified

Machine validation: `pending`

Conformance level: `authoring_aligned`

This ledger keeps rendered, destination and governance exceptions open instead of
upgrading source inspection into stronger evidence.

| ID | Result | Evidence or open reason |
|---|---|---|
| SC-01 | pass | Both locale roots carry the required identity attributes and four `landometer:*` receipt metas; local resource existence and unique-ID checks passed. |
| SC-02 | pass | The inline theme initializer precedes every stylesheet and resolves stored light/dark preference or the system theme. |
| SC-03 | fail | Canonical kit bytes match their recorded hashes, but the imported canonical extension registry contains raw values outside Appendix E; the page-local layer adds none. |
| SC-04 | pass | Text actions and the theme/language preferences use capsule geometry; icon-only exits use circles. The topic picker keeps native field geometry and is not treated as a third action shape. |
| SC-05 | pass | Foreground contracts, focus styles, non-modal menu focus/restoration, disclosure, listbox, form and keyboard paths passed local implementation QA. In dark mode, menu and topic-option hover changed from transparent to `color(srgb .244784 .393333 .443686)` with light text. |
| SC-06 | pass | The reduced-motion contract disables reveals, disclosure animation, the hero pan, hero parallax, substantial-static-image parallax and CTA sweep, keeps the navbar prominent, and retains final visible states; no-JavaScript content starts visible. |
| SC-07 | pass | Source minimums are present; local TH/EN rendering at widths 320, 360, 390, 600, 768, 900, 1080, 1081, 1280 and 1440 confirmed zero `.btn` and page overflow, all computed nowrap and no Location CTA above 48 px. |
| SC-08 | pass | Each locale has one H1, matching language, header/main/footer landmarks and a first-source skip link. |
| SC-09 | pass | Atmosphere gradients resolve through canonical DS recipes. The only page-local raw colour declaration is the owner-approved r6 light-wordmark identity value, centralized once and recorded in the identity contract. |
| SC-10 | fail | `site.css` has three narrowly scoped `!important` declarations needed to normalize TikTok's provider-owned embed width and two local custom properties for header geometry and approved identity colour; this is not a literal all-properties-through-`var()` pass. |
| SC-11 | pass | Primary copy is static HTML and initial page rendering is same-origin/self-hosted. Actual provider surfaces attach only when their cards approach the viewport in both light and dark themes. DS-owned shells follow the selected theme; cross-origin provider internals retain provider styling. The contact POST occurs only after explicit submit. |
| SC-12 | pass | Both locale routes are indexable and carry canonical/hreflang links, Open Graph and Twitter metadata, Organization/WebSite/WebPage JSON-LD, the r6 favicon, `robots.txt`, and a truthful release-dated sitemap. |
| SC-13 | pass | TikTok, Facebook, Instagram and LinkedIn use rounded-outline marks in a readable editorial grid. Real provider content loaded in dark and dark→light→dark preserved every iframe source and TikTok loaded state; DS-owned shells changed theme while provider internals retained provider styling. No substitute appeared, and the 390 px Instagram geometry had no fake blank tail. The X embed remains omitted while its icon-only outbound footer link remains. |
| SC-14 | fail | The 108 native raw / 101 enhanced source inventory plus two keyboard-focusable horizontal rails, local keyboard/form states and explicit 44 px `mailto:` action are checked, but a real contact POST/inbox outcome is not included. Runtime loop clones are sanitized, inert and excluded from focusable-control counts. |
| SC-15 | pass | GitHub Pages deployment `6168455185` succeeded for artifact commit `ce107609`; root, index, TH, EN and `_ds` CSS returned HTTP 200, and seven live HTML/CSS/JS resources matched local bytes and SHA-256. Targeted live TH dark checks at 320/390/1440 confirmed the ui11 build, one-line Location CTA fit, dark hover, all four real provider surfaces, theme-state retention, rounded-outline footer marks, zero overflow and 3/3 logical carousel reveal parity. |
| SC-16 | fail | The shared manifest records both locale routes but is not claimed as a complete per-route rendered-contract projection. |
| SC-17 | pass | Retired values are absent from the public page layer; no retired literal was added in this candidate. |
| SC-18 | pass | The first view has no caution/caveat block; the dated product proof band follows the hero and the null legacy row is not rendered. |
| SC-19 | pass | Computed local QA confirmed every `.icon-symbol` uses Material Symbols Rounded at FILL 0 / wght 300. Bookmark roles retain unique outline glyphs; all social SVGs use fill none, 1.65 px strokes and round caps/joins, and icon-only footer targets are 44 px with hidden accessible labels. Product marks remain identity assets. |
| SC-20 | fail | Targeted `ui-20260830-11` checks passed TH/EN widths 320–1440, dark hover/focus, one-line Location CTA fit, loop reveal parity, real dark-theme provider content, theme switching, icon contracts, zero overflow, local HTTP/MIME, syntax, diff, JSON/YAML, resources and unique IDs. The exhaustive locale × light/dark/system × viewport × zoom/text-scale/reduced-motion/blocked-provider matrix remains open. |
| SC-21 | pass | The navbar is 76/68 px when prominent and scales the complete row to a 29/27 px calm state; it restores on upward scroll, pointer/focus intent and menu open. Mobile shows only the carrier-free brand and circular menu control, while the one-line sign-in action moves inside the full-width menu. |
| SC-22 | pass | Source inspection confirms the approved reveals, story staging, four-beat rally, CTA sweep and 32-second hero pan remain. Hero parallax stays capped at ±20 px; substantial static content images receive separately bounded scroll-linked movement; every layer freezes under reduced motion. Social provider surfaces lazy-load in both themes. At 390/768/1440, both rails crossed left/right boundaries while every logical reveal group remained exclusively 0/3 or 3/3, never mixed. The owner-directed ambient video loop remains an explicit divergence from canonical no-loop guidance. |
| SC-23 | pass | Local rendered QA confirms the rounded-outline interface/social icon contract, stronger dark hover/focus surface, shortened one-line Location CTA captions, and sanitized inert loop clones with source/clone reveal parity. Full exhaustive coverage remains open under SC-20. |

Totals: 18 pass · 5 fail/open · 0 not applicable.
